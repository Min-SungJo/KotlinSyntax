package ex20221121
/**논리연산자*/
fun main() {
    // 5) 논리 연산자 && || !

    // && : 양쪽 항이 모두 true 일 경우 결과 값으로 true 를 돌려준다.

    // || : 하나라도 true 일 경우 결과 값으로 true 를 돌려준다.

    // ! (not) 부정연산자 : true 앞에 붙이면 반대로 출력 !true ---> false
}